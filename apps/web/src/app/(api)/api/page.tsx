// SPRINT-1: API surface placeholder — serves at `/api` until Sprint 2 adds route handlers.
// No API route handlers in this sprint.
export default function ApiPlaceholderPage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>API</h1>
      <p>Route handlers live under this group from Sprint 2 onward (placeholder).</p>
    </main>
  );
}
