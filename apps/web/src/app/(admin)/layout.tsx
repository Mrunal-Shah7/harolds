// SPRINT-1: admin route-group layout.
// Sprint ownership: Admin back-office UI is filled in Sprint 8.
export default function AdminLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
