// SPRINT-1: admin placeholder — serves at `/admin` (group name not in URL).
export default function AdminPage() {
  return (
    <main style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>Admin</h1>
      <p>Back-office — menu, hours, orders, reports (placeholder).</p>
    </main>
  );
}
